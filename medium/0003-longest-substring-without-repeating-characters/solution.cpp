            }
                left++;
                st.erase(s[left]);
            if(st.find(s[right]) !=st.end() ){
        for (int right = 0; right<s.length(); right++){

        int result = 0;
        int left =0;
        set<char>st ;
    int lengthOfLongestSubstring(string s) {
public:
class Solution {
